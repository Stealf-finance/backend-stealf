# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/stealf_backend

# CORS Configuration
CORS_ORIGIN=*

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet
SOLANA_PROGRAM_ID=A5GtBtbNA3teSioCX2H3pqHncEqMPsnHxzzXYPFCzTA4

# Umbra Privacy Configuration
UMBRA_RELAYER_URL=https://relayer.umbraprivacy.com/
UMBRA_INDEXER_URL=https://5nqw12m1pa.execute-api.eu-central-1.amazonaws.com/proof/

# Simple Mixer Configuration (Alternative to Umbra - No ZK required)
# Pool wallet private key (base58-encoded)
# SECURITY: Keep this secret! Never commit to version control!
# Generate with: solana-keygen new
# Leave empty to auto-generate temporary wallet (development only)
# MIXER_POOL_PRIVATE_KEY=

# Enable standardized pool sizes (improves privacy)
# When enabled, only accept deposits of standard amounts (0.1, 1, 5, 10 SOL)
MIXER_STANDARDIZED_POOLS=false

# ZK Proof Artifacts (REQUIRED - see ZK_ARTIFACTS_BLOCKER.md)
# URL base where ZK proof artifacts (.wasm, .zkey, .json files) are hosted
# Examples:
# - Production CDN: https://cdn.umbraprivacy.com/zk
# - Local dev server: http://localhost:3001/zk
# - Custom CDN: https://your-cdn.com/umbra-artifacts
# ZK_ARTIFACTS_BASE_URL=NOT_CONFIGURED

# Arcium Encrypted Transfers Configuration
# Enable Arcium MPC-powered encrypted transfers (amounts hidden on-chain)
ENABLE_ARCIUM_TRANSFERS=false
# Arcium program ID (set after deployment)
# ARCIUM_PROGRAM_ID=

# Security (Production only)
ENCRYPTION_KEY=change-this-to-a-strong-random-key-in-production

# Telegram Bot (Optional)
ENABLE_TELEGRAM=false
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
ADMIN_CHANNEL_ID=YOUR_ADMIN_CHANNEL_ID
